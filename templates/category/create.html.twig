{% extends "base.html.twig" %}

 {% block body %}
    <div class="container">
    <h2>Create a new category</h2>
    <div>
        {{form_start (form)}}
            <div class="form-group">
                {{form_label (form.name)}}
                {{form_widget (form.name, {'atrr':{'class': 'form-control'}})}}
            </div>
            <div class="form-group">
                <button class="btn btn-primary mt-3">Create</button>
            </div>
        {{form_end (form)}}
    </div>
    </div>
 {% endblock %}